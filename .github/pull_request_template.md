<!--
Thanks for contributing. CONTRIBUTING.md has the build, the test suite, and
the layering rules if anything below is unclear.
-->

## What this changes

<!-- One or two sentences. Link an issue if there is one. -->

## Checklist

- [ ] Tests added or updated; a bug fix has a test that failed before it
- [ ] Any new header is listed in its library's `FILE_SET HEADERS`, and in `owl.h` if it is public
- [ ] Full `ctest` passes locally
- [ ] README updated if the public API changed
- [ ] `compile_fail/` tests still fail to compile
